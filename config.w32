// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("zyconf", "for zyconf support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("zyconf", "enable zyconf support", "no");

if (PHP_ZYCONF != "no") {
	EXTENSION("zyconf", "zyconf.c");
}

